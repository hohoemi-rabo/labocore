# LaboCore フェーズ2 要件定義書「授業記録(じゅぎょうのきろく)」v1.0

- 作成日: 2026-08-16
- 発注者: ほほ笑みラボ(まさゆきさん)
- 根拠資料:
  - LaboCore 実装仕様書フェーズ1(SPEC.md・as-built)
  - 授業記録サイト 要件定義書 v0.1(2026-08-11・単独サイト前提の旧版)
  - フェーズ2ヒアリング(2026-08-16)
  - 教室アンケート集計(2026年7〜8月・20名回答)

---

## 1. 背景と目的

- 教室アンケートで「復習したい・前にやったことを見返したい」という声が最多だった
- 授業はプリント・教科書を使わないスタイルで、近年はAI中心。AIは変化が速く、詳細な手順書はすぐ古くなるため、**「前にこんなことをやりました」が分かる授業の記録(ログ)**を提供する
- 生徒さんがスマホでウェブページを見る・ホーム画面に追加する練習の教材も兼ねる
- 本機能は、稼働中の教室運営システム **LaboCore(Next.js + Supabase + Vercel)のフェーズ2として統合実装**する。旧v0.1の「静的サイト・DBなし」方針は本書で正式に撤回する(→3章)

### 成功のイメージ

- 生徒さんが「前のあれ、何だっけ」を自分のスマホで確認できる
- まさゆきさんの更新作業が1回あたり数分以内で、3ヶ月以上続けられる
- 他クラスの活動が見えることで、教室の楽しさが伝わる(宣伝効果)

## 2. フェーズ2のスコープ

### 含むもの(ヒアリングにより全部入りで確定)

1. 生徒向け授業記録サイト一式(合言葉 → クラスえらび → クラスページ)
2. 記録カード(日付・テーマ・ひとことメモ・写真・プロンプト+コピーボタン)
3. **AI下書き生成**(メモ+写真からGemini APIがカードの下書きを作成)
4. 記録カードの**他クラスへのコピー(下書き複製)**機能
5. 次回のじゅぎょう(クラスごと)
6. 今月のよてい(コマ曜日+休講日から自動生成)
7. 教室からのお知らせ(全体/クラス別・掲載期間つき)
8. PWA対応(ホーム画面に追加・専用アイコン)
9. **LaboCore全体のデザイン全面刷新**(管理画面含む・→12章)

### 含まないもの(フェーズ3以降の候補)

- 見積書・請求書・領収書のPDF作成
- 事前欠席連絡、生徒向け出席閲覧、LINE通知連携
- つまずきカルテ連携、過去データ取込

## 3. v0.1からの前提変更点(重要)

| 項目 | 旧v0.1(単独サイト案) | 本書(LaboCore統合) |
|---|---|---|
| 基盤 | 静的サイト+無料ホスティング | LaboCore(Next.js 15 / Vercel)に統合 |
| データ | JSONファイル | Supabase PostgreSQL(既存DBにテーブル追加) |
| クラス一覧 | JSONで独自管理 | 既存 `classes` テーブルを共用 |
| 更新方法 | 素材→GitHub Actionsで自動生成 | LaboCore管理画面の入力フォーム+**AI下書き生成** |
| 画像 | リポジトリ内の画像ファイル | **Cloudflare R2**(無料枠)に保存・配信 |
| 費用 | 完全無料 | **完全無料を維持**(AI下書きはGemini APIの無料枠で運用・→9章) |
| デザイン | 生徒向けサイトのみBMW M系 | **LaboCore全体をBMW M系に刷新** |

変わらないもの: 合言葉「ほほえみ」/ 顔を写さない写真運用 / noindex / 記録カードの項目構成 / LINEグループでの更新告知(サイト内通知は作らない)。

## 4. 利用者と認証の2系統

| 利用者 | 入り口 | 認証 |
|---|---|---|
| 生徒さん(閲覧のみ) | `/kiroku` 配下 | 全員共通の合言葉「ほほえみ」のみ。当面ハードコード相当(環境変数)で固定・変更UIなし |
| まさゆきさん(管理) | 既存の管理画面 | 現行どおり Supabase Auth(メール+パスワード・1名) |

- 合言葉は一度入力したらブラウザに記憶し、次回から省略(Cookie)
- 間違えたときは、やさしい日本語でやり直し案内
- **セキュリティの位置づけ(正直な注記)**: 合言葉はアプリ層のゲートであり、授業記録系テーブルはanonロールに読み取りを許可するため、技術的にはAPIを直接叩けば合言葉なしで読める。掲載内容は「顔なし・個人情報なしの授業記録」に限定する運用のため、**「身内向けの目印」で十分**という従来方針を維持する。生徒台帳・出欠・支払い等の既存テーブルはこれまでどおりanon完全遮断

## 5. URL・ルーティング・アクセス制御

- 生徒向けURL: **`https://labocore.vercel.app/kiroku`**(LINEグループで案内)
- Next.js のroute groupを追加(例: `(kiroku)`)。既存 `(auth)` `(app)` とは独立した生徒向けレイアウト(新デザイン・管理ナビなし)
- `src/middleware.ts` のmatcher/分岐を改修: `/kiroku` 配下はSupabase Auth必須の対象から外し、代わりに**合言葉Cookieの有無で判定**(未入力なら合言葉画面へ)。`/api/keepalive` の除外前例に倣う
- 生徒向け全ページに `noindex`(metadata `robots`)を設定
- 管理画面のルーティング・保護は現行のまま

## 6. クラスデータの共通化

- 生徒向け「クラスえらび」「クラスタブ」は既存 **`classes` テーブルをそのまま使用**(is_active=true のみ、weekday→start_time順)。クラス名も現行のコマ名をそのまま生徒に表示する
- **水曜午後クラスの追加は、既存のコマ管理画面(`/settings/classes`)から登録するだけ**で対応可能(フェーズ1の機能で足りるため新規開発不要)。登録すれば生徒向け画面のボタン・タブにも自動反映される(旧v0.1の拡張性要件をDBで実現)
- `classes` に列を追加: **`theme_color`(テーマカラー・手動設定)**。コマ管理フォームに色の選択欄を追加し、生徒向け画面はクラスごとにこの差し色で表示する
- anonに読み取りを許可する列は運用上、コマ名・曜日・時間・テーマカラー等の非個人情報のみ(生徒情報とは無関係のテーブルのため実害なし)

## 7. 生徒向け画面仕様(`/kiroku`)

### 画面K1: 合言葉

- ひらがなの合言葉「ほほえみ」を入力。正解でCookieを保存し、以後スキップ
- 誤入力時はやさしい文言で再入力を促す(回数制限などは設けない)

### 画面K2: クラスえらび(初回のみ)

- 「いつも来ている曜日(クラス)」を大きなボタンで選ぶ。クラス名+曜日+時間帯を併記、各ボタンにテーマカラー
- 選択はCookieに記憶し、次回からは自分のクラスのページを直接開く(タブでいつでも変更可能)

### 画面K3: クラスのページ

上部にクラスタブ(全active クラス・`classes` から自動生成)。**他クラスの記録も自由にのぞける**。選択中クラスのテーマカラーが画面の差し色に反映される。本文は上から:

1. **教室からのお知らせ**(掲載期間内のものだけ表示。全体向け+このクラス向け)
2. **次回のじゅぎょう**(日時・テーマ・持ち物など・目立つデザイン)
3. **今月のよてい**(このクラスの曜日の開講日と、`closed_days` の休講日を月表示。祝日の自動判定はせず、休みたい祝日は休講日として登録する現行運用のまま)
4. **これまでのじゅぎょう**(公開済みの記録カードを新しい順)

### 記録カード(1授業=1件)

| 項目 | 必須/任意 | 内容 |
|---|---|---|
| 日付 | 必須 | 例: 8月6日(木)。既存 `formatDateWithWeekday` を利用 |
| テーマ | 必須 | 例: AIで暑中見舞いを作りました |
| ひとことメモ | 必須 | 2〜3文。授業の様子が伝わる文章 |
| 画面写真 | 任意 | 1〜2枚(R2から配信) |
| プロンプト | 任意 | その日AIに送った指示文。**コピーボタン付き**(家で同じ体験を再現できる) |

### PWA

- Webマニフェスト+教室アイコンを用意し、「ホーム画面に追加」で1タップ起動(スコープは `/kiroku`)
- iPhone(Safari)/ Android(Chrome)双方で動作確認。追加のしかたは授業で練習(教材化)
- オフライン閲覧は必須としない(無理なく入れられれば対応)

## 8. 管理側画面仕様(LaboCoreに追加)

ナビ(サイドバー/下部タブ)に「記録」を追加。既存のCRUDパターン(Zod検証・`{fieldErrors|formError}`・revalidate+redirect)とインライン更新パターン(§9 of SPEC)を踏襲する。

### 8.1 記録カード管理

- 一覧: クラス×新しい順。下書き/公開の状態が分かる
- 新規作成フォーム: クラス選択・日付(既定=今日)・テーマ・ひとことメモ・写真アップロード(最大2枚)・プロンプト(任意)・状態(下書き/公開)
- **AI下書き生成**(→9章): メモ(走り書きでよい)+写真をアップして「AIに下書きを作ってもらう」ボタン → テーマ案・清書されたひとことメモがフォームに反映 → 手直しして公開
- **他クラスへコピー**: 既存カードから「別のクラスに複製」→ クラスと日付を選ぶと下書きが作られ、手直しして公開(同テーマを複数クラスでやる週の負荷を下げる)
- 編集・削除(削除は物理削除+`ConfirmDialog`)

### 8.2 次回のじゅぎょう

- クラスごとに1件(日時・テーマ・持ち物メモ)を編集する簡易フォーム。過去日になったものは生徒向けに表示しない

### 8.3 お知らせ管理

- 項目: タイトル・本文・対象(全体/特定クラス)・掲載開始日・掲載終了日
- **掲載期間を過ぎたお知らせは自動で非表示**(消し忘れ防止)。一覧では期限切れをmuted表示
- CRUD(削除は物理削除)

## 9. AI下書き生成(Google Gemini API組み込み)

### フロー

1. まさゆきさんが授業後、フォームに走り書きメモを入れ、スクショをアップ
2. 「AIに下書きを作ってもらう」→ Server ActionがGemini APIを呼ぶ(画像+メモを送信)
3. AIが「テーマ(短い見出し)」「ひとことメモ(です・ます調2〜3文・教室の温かいトーン)」を生成し、フォームに反映
4. **まさゆきさんが必ず確認・手直しして公開**(AIの出力をそのまま自動公開しない)

### 要件・条件

- モデルは **Gemini Flash系(現行世代のFlash / Flash-Lite)** を第一候補とする(モデルID・生成プロンプトは設計工程で確定)
- **費用: 無料枠内で運用する**。Flash系モデルにはAPI無料枠があり(目安: 1日あたり千件規模・分あたり十数件のリクエスト上限)、週6回程度の本用途には十分。これにより**システム全体の「完全無料」方針を維持できる**
- **モデルIDは環境変数等で差し替え可能な作り**にする(Geminiは旧世代モデルの提供終了が比較的早いため、モデル名のハードコードを避け、世代交代時にコード改修なしで移行できるようにする)
- APIキーは環境変数 `GEMINI_API_KEY` としてVercelに設定(サーバー側のみで使用し、クライアントに露出させない)
- **データの取り扱いに関する運用ルール**: 無料枠に送信した入力はGoogleの製品改善に利用されうる。掲載素材は元々「顔・個人情報を写さない」方針だが、これに加えて**個人情報を含むスクショ・メモをAIに送らない**ことを運用ルールとして明記する
- 送信画像はリサイズしてから送る(通信量節約・→11章の圧縮処理を共用)
- API障害・レート制限時はエラートーストを出し、**手動入力だけでも完結できる**設計とする(AIはあくまで補助)

## 10. データモデル追加(Supabase)

SPEC §13の拡張ルールに従う: 全新テーブルでRLS有効化 / 追加後に `database.types.ts` をMCPで再生成 / 検索列にインデックス。

### 10.1 `classes` への列追加

| 列 | 型 | 内容 |
|---|---|---|
| `theme_color` | text | クラスのテーマカラー(手動設定。形式・候補パレットは設計工程で決定) |
| `next_lesson_date` | date, nullable | 次回のじゅぎょう: 日付 |
| `next_lesson_theme` | text, nullable | 同: テーマ |
| `next_lesson_note` | text, nullable | 同: 持ち物などのメモ |

(次回情報はクラスごとに常に1件のため、専用テーブルを作らず `classes` に持たせて構造を簡素に保つ)

### 10.2 `lesson_records`(記録カード)

| 列 | 型 | 制約・既定 |
|---|---|---|
| `id` | uuid | PK, `gen_random_uuid()` |
| `class_id` | uuid | NOT NULL, FK → classes.id |
| `lesson_date` | date | NOT NULL |
| `theme` | text | NOT NULL |
| `memo` | text | NOT NULL(ひとことメモ) |
| `prompt` | text | nullable(コピーボタン用) |
| `image_urls` | text[] | 既定 `'{}'`(最大2件。R2の公開URLを完全な形で保存し、将来の配信先変更を容易にする) |
| `status` | text | NOT NULL, CHECK in ('draft','published'), 既定 'draft' |
| `created_at` / `updated_at` | timestamptz | 既定 `now()` |

- **UNIQUE(`class_id`, `lesson_date`)** — 1クラス1日1件
- インデックス: `idx_lesson_records_class_date (class_id, lesson_date DESC)`

### 10.3 `announcements`(教室からのお知らせ)

| 列 | 型 | 制約・既定 |
|---|---|---|
| `id` | uuid | PK |
| `title` | text | NOT NULL |
| `body` | text | NOT NULL |
| `class_id` | uuid | nullable, FK → classes.id(**NULL=全体向け**) |
| `starts_on` / `ends_on` | date | NOT NULL(掲載期間) |
| `created_at` | timestamptz | 既定 `now()` |

- インデックス: `idx_announcements_ends_on (ends_on)`

### 10.4 RLSポリシー方針

- 管理操作: 既存と同型の `authenticated_all`(認証済み=全操作可)を両新テーブルに付与
- 生徒閲覧(anon SELECT のみ許可):
  - `classes`: `is_active = true` の行のSELECTを許可
  - `lesson_records`: `status = 'published'` の行のみSELECT許可(下書きはDBレベルで見えない)
  - `announcements`: 掲載期間内(`starts_on <= today <= ends_on`)の行のみSELECT許可
  - `closed_days`: SELECT許可(今月のよてい表示用。休講日情報に個人情報なし)
- `students` / `attendance_records` / `payments` はanon遮断を現状維持(変更しない)

## 11. 画像の保存・配信(Cloudflare R2)

- 写真は **Cloudflare R2** に保存する(無料枠: 容量10GB・**配信の帯域課金なし**。この規模では実質恒久無料)
- アップロードはServer ActionからS3互換APIで実施。認証情報は環境変数(`R2_ACCOUNT_ID` / `R2_ACCESS_KEY_ID` / `R2_SECRET_ACCESS_KEY` / `R2_BUCKET` / `R2_PUBLIC_BASE_URL`)
- 公開配信は当面 `*.r2.dev` の公開URL(無料)で行う。**注記**: r2.devは簡易配信向けでレート制限があるが、生徒約20名規模なら実用上問題になりにくい。将来問題が出た場合の移行(Supabase Storage等)に備え、DBにはURLを完全な形で保存する
- アップロード時にサーバー側で**リサイズ+WebP圧縮**(長辺1200px目安)を行い、容量とスマホでの表示速度を両立する。この処理はAI送信用の縮小(9章)と共用する
- 掲載前チェックの運用ルールを維持: 画面・資料のみ(顔・名前を写さない)/ メールアドレス等の写り込み確認

## 12. デザイン全面刷新(管理画面を含む)

ヒアリングにより、**生徒向けページだけでなくLaboCore全体(出欠・カレンダー・集計・設定)を新デザインへ刷新**することが確定(工数増を許容)。

### 方向性

- ダーク基調(#0b0d12系)+クラス別の鮮やかな差し色。影・グラデーション・ガラス面で立体感を出す(具体値はDESIGN.md v2)
- 生徒向け画面は**選んだクラスのテーマカラーが差し色になる**(6クラス分の色はサンプルで確定済み・DESIGN.md v2の3章)
- ブランドモチーフとしてトリコロールバー(スカイ/ブルー/ローズ)を全画面共通で使用
- 「シニア向けらしい」見た目にしない。文言は漢字を普通に使い、子ども向けにしない
- 構成は縦一列のシンプルさを保つ(装飾は凝る・構造は簡素に)

### 現行デザインルールの改訂

- フェーズ1の絶対ルールのうち「第2アクセント禁止・box-shadow禁止・グラデーション禁止」は**廃止**し、新デザインシステムに置き換える
- **維持するもの**: タップ要素44×44px以上 / 大きめ文字・高コントラスト(本文17px基準)/ `tabular-nums` / レスポンシブ(375px〜)/ トークン管理(インラインhex禁止)
- 文字サイズ変更ボタン・印刷機能は設けない(従来方針どおり)

### 進め方

- **見た目の正典は、まさゆきさん承認済みのデザインサンプル(`design-sample.html`)とし、リポジトリに `docs/design-sample.html` として同梱する**。BMW MのデザインシステムURLは参考情報に格下げする(外部URLは消える可能性があるため)
- 新デザインの仕様書として **DESIGN.md v2** を設計工程の最初に確定する(サンプルから書き起こし済みのたたき台あり。カラートークン・タイポ・角丸・影のレシピ・クラステーマカラー6色・管理画面への展開方針を定義)
- 管理画面の刷新は既存機能のリグレッションリスクがあるため、**画面単位で段階的に**置き換える(→16章のマイルストーン)。移行期間中の新旧混在は画面単位でのみ許容する

## 13. 非機能・運用

### 費用まとめ

| 項目 | サービス | 費用 |
|---|---|---|
| ホスティング | Vercel(既存) | 無料 |
| DB・認証 | Supabase(既存) | 無料 |
| 画像 | Cloudflare R2 | 無料(無料枠内) |
| AI下書き | Google Gemini API(Flash系) | 無料(無料枠内で運用・→9章) |
| ドメイン | labocore.vercel.app のまま | 無料 |

**→ ランニングコストは合計0円(完全無料方針を維持)**

### 運用

- 更新頻度: 最大週6回(クラス数と同じ)。目標作業時間は1回あたり数分以内(メモ+写真アップ→AI下書き→手直し→公開)
- 更新の告知はサイトに作らず、**LINEグループ投稿**で行う(現行方針のまま)
- keepalive cron等のフェーズ1運用はそのまま維持。`/kiroku` 追加に伴うmiddleware matcher変更時に、keepalive除外が壊れていないことを確認する
- 生徒さんのスマホ内訳は `students.smartphone_os` に入力を進め、動作確認の優先順位に使う

## 14. 維持するドメイン不変条件

SPEC §5の不変条件(単価スナップショット方式 / 出欠1日1件 / 論理削除 / JS集約 / 休講日は出欠対象外 / 円・整数)は、フェーズ2で一切変更しない。授業記録機能は出欠・請求ロジックに影響を与えない。

## 15. スコープ外(今回はやらないこと)

- 予約・出欠の生徒側操作、質問受付・コメント機能
- 生徒個別のログイン・会員管理(合言葉のみ)
- 決済・月謝管理の変更
- 更新お知らせの自動配信(LINE投稿で代替)
- スマホ専用アプリ(PWA+レスポンシブで両対応)
- 合言葉の変更UI(当面固定)
- 祝日の自動判定(休講日登録の現行運用で代替)
- AI出力の自動公開(必ず人の確認を挟む)

## 16. 実装マイルストーン(推奨)

| M | 内容 | ねらい |
|---|---|---|
| M1 | DESIGN.md v2策定 / DB追加(テーブル・RLS・型再生成)/ R2セットアップ / 管理側の記録・次回・お知らせ入力(手動)/ 生徒向け `/kiroku` 一式(新デザイン・PWA) | **生徒に見せられる状態を最短で作る**。水曜午後クラスの登録・LINEでのURL+合言葉案内・数名への先行お披露目まで |
| M2 | AI下書き生成 / 他クラスへのコピー機能 | 週6回運用の負荷を下げる |
| M3 | 既存管理画面(出欠・カレンダー・集計・設定)の新デザインへの段階的刷新 | リグレッションを抑えながら全面刷新を完了 |

- 開始時期・各マイルストーンの期日はまさゆきさんと調整(旧v0.1の「8月中に最小構成」を踏まえるならM1を8月中が目安)

## 17. 未決事項

- [x] サイト名 →「**ほほ笑みラボ 授業の記録**」で確定(2026-08-16・サンプル表記のまま)。生徒向けアイコンは実装時にデザインシステム準拠で作成(後日画像差し替え可・チケット21)
- [x] クラステーマカラー → サンプルの6色で確定(月ローズ/火オレンジ/水午前スカイ/水午後インディゴ/木エメラルド/金フクシア。DESIGN.md v2の3章)
- [x] AI下書きの生成プロンプト・モデル最終選定 → **`gemini-3.5-flash-lite`**(2026-08-17・チケット22)。生成プロンプトは `src/lib/gemini.ts` の `SYSTEM_INSTRUCTION` に定数として置き、モデルは env `GEMINI_MODEL` でコード改修なしに差し替えられる。無料枠内・実測で画像1枚込み約2秒/1,434トークン
- [x] 過去の記録が増えたときの探し方 → **当面は新しい順のみで運用する**(2026-08-17 時点の結論)。生徒向けは記録が0件からの立ち上がりで、まだ探しづらさが出ていない。実際に「見つからない」が起きてから月区切り・タグを検討する
- [x] M1〜M3の具体的な日程 → **すべて 2026-08 に完了**(M1=13〜21 / M2=22〜23 / M3=24〜28)。フェーズ2はチケット28 をもって完了
- [x] 合言葉 →「ほほえみ」固定(変更UIなし)
- [x] AIのAPI → **Google Gemini API**に確定(無料枠で運用・2026-08-16)
- [x] 画像の保存先 → Cloudflare R2
- [x] デザイン刷新の範囲 → 管理画面含む全面刷新(段階的に実施)
