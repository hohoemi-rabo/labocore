# LaboCore DESIGN.md v2「じゅぎょうのきろく」デザインシステム

- 作成日: 2026-08-16
- ステータス: **確定**(2026-08-16 まさゆきさん承認。フェーズ2以降のUI/UXはすべて本書に従う)
- **見た目の正典**: まさゆきさん承認済みのサンプル `docs/design-sample.html`(同梱済み)。本書はそのサンプルからトークン・部品を書き起こしたもの。数値が食い違う場合はサンプルを優先し、本書を直す
- 適用範囲: フェーズ2以降のLaboCore全体(生徒向け `/kiroku` + 管理画面。管理画面は画面単位で段階移行)

---

## 0. v1からの変更点

| 項目 | v1(フェーズ1) | v2(本書) |
|---|---|---|
| 基調 | ライト(白ベース+Action Blue) | **ダーク**(#0b0d12ベース+クラス別アクセント) |
| box-shadow | 禁止 | **多用する**(奥行きの主役。→4章のレシピに従う) |
| グラデーション | 禁止 | **多用する**(面・ボタン・アクセント) |
| 第2アクセント | 禁止 | クラスカラー6色+機能色(お知らせ=琥珀、プロンプト=紫)を役割固定で使用 |
| フォント | Inter + Noto Sans JP(400/600) | **Noto Sans JP**(400/500/700/900) |
| 角丸 | 8/18/9999のみ | 12/14/16/20/26/999の段階制(→6章) |

### v1から維持するもの(不変)

- タップ要素 **最小44×44px**(サンプルのタブ等は実装時に `min-height: 44px` へ引き上げる)
- 本文17px基準・16px以下の本文禁止
- 数字・金額は `tabular-nums`
- デザイントークンは `tailwind.config.ts` の `theme.extend` に登録し、**インラインhex禁止**
- レスポンシブ(375px〜)・`active:scale-95` 等のタップフィードバック

## 1. デザイン原則

1. **装飾は凝る、構造は簡素に**: レイアウトは縦一列(max-width 680px中央)。豪華さは面・影・色で出し、情報構造では出さない
2. **クラスカラーが主役**: 選んだクラスの色が画面全体の差し色(`--accent`)になる。色で「自分のクラスにいる」ことが分かる
3. **奥行きは3点セット**: ①ガラス面(白の微グラデーション+1pxライン)、②大きく柔らかい落ち影、③上端1pxのインセットハイライト。この3つの組み合わせで統一する
4. **役割ごとに色を固定**: お知らせ=琥珀、プロンプト=紫、休み・エラー=ローズ、完了=緑。役割外に流用しない
5. **シニア向けらしくしない**: 漢字は普通に使う。文字は大きく、コントラストは高く、しかし見た目は現代的に

## 2. カラートークン

### 基礎色

| トークン | 値 | 用途 |
|---|---|---|
| `bg` | `#0b0d12` | ページ最下層の背景 |
| `surface` | `#161b26` | カード面(暗端) |
| `surface-2` | `#1d2433` | カード面(明端。グラデーションの起点) |
| `text` | `#f2f4f8` | 見出し・本文 |
| `text-body` | `#d6dbe6` | カード内本文(記録カードのメモ等) |
| `sub` | `#9aa3b5` | 補助テキスト・キャプション |
| `line` | `rgba(255,255,255,.08)` | 枠線・区切り(1px) |

### アクセント(動的)

| トークン | 値 | 用途 |
|---|---|---|
| `--accent` | 選択中クラスの `theme_color`(既定 `#38bdf8`) | 差し色の本体。eyebrow文字・次回カードの縁と光・日付ピル・ブランドマーク |
| `--accent-soft` | `color-mix(in srgb, var(--accent) 16%, transparent)` | フォーカスリング・淡い面 |
| 濃色端の生成 | `color-mix(in srgb, var(--accent) 55%, #000)` | グラデーションの暗い側は必ずこの式で作る(専用色を増やさない) |

### 機能色(役割固定)

| 役割 | 値 |
|---|---|
| お知らせ(琥珀) | 文字 `#fbbf24` / 面 `rgba(251,191,36,.14)→rgba(251,146,60,.05)` / 縁 `rgba(251,191,36,.35)` / 本文 `#e6d9bd` |
| プロンプト(紫) | `violet: #8b7cf6` / コピーボタン `#8b7cf6→#6d28d9` / pre文字 `#e8e4ff` |
| 休み・エラー(ローズ) | 文字 `#fda4af` / 面 `rgba(225,29,72,.12)` / 縁 `rgba(225,29,72,.35)` |
| 完了(緑) | `#34d399→#059669`(コピー完了時のボタン) |

### トリコロール(ブランドモチーフ)

- `linear-gradient(90deg, #4fc3f7 0 33%, #1e5bd6 33% 66%, #e11d48 66% 100%)`
- 高さ4px・幅56〜76px・角丸999のバー。入口ボックス・ページ見出し横・フッターに使用。BMW Mオマージュの要であり、**このモチーフは全画面で共通**

## 3. クラステーマカラー

`classes.theme_color` に保存し、選択中クラスの色を `--accent` に注入する。

| クラス | 色 | 系統 |
|---|---|---|
| 月曜 | `#f43f5e` | ローズ |
| 火曜 | `#fb923c` | オレンジ |
| 水曜(午前) | `#38bdf8` | スカイ |
| 水曜(午後) | `#818cf8` | インディゴ |
| 木曜 | `#34d399` | エメラルド |
| 金曜 | `#e879f9` | フクシア |

- 新クラス追加時は上記と同じ「明るく彩度の高い、ダーク背景で映える」帯から選ぶ(コマ管理フォームの色選択はこのパレットを候補提示)
- 濃色端は必ず `color-mix(... 55%, #000)` で導出し、クラスごとに2色目を定義しない

## 4. 背景・面・立体感

### ページ背景(アンビエント)

```css
background:
  radial-gradient(900px 500px at 85% -10%, rgba(37,99,235,.20), transparent 60%),
  radial-gradient(700px 420px at -10% 100%, rgba(225,29,72,.12), transparent 60%),
  var(--bg);
background-attachment: fixed;
```
右上に青・左下にローズのにじみを固定配置(入口画面は少し強め: .25/.16)。

### ガラス面(汎用カード)

- 面: `linear-gradient(160〜165deg, rgba(255,255,255,.045〜.06), rgba(255,255,255,.012〜.015))`
- 縁: `1px solid var(--line)`
- 入口ボックスとヘッダーは `backdrop-filter: blur(12〜14px)` を併用

### 影のレシピ(3段階+インセット)

| 段階 | 値 | 用途 |
|---|---|---|
| 小 | `0 10px 24px rgba(0,0,0,.35)` | ボタン・小カード |
| 中 | `0 16px 40px rgba(0,0,0,.42)` | 記録カード等の主要カード |
| 大 | `0 24px 60px rgba(0,0,0,.55)` | 入口ボックス・ヒーロー |
| 上端ハイライト | `0 1px 0 rgba(255,255,255,.05〜.06) inset` | 中・大に併用 |
| 色付きグロー | `0 8〜10px 20〜24px color-mix(in srgb, <色> 35〜45%, transparent)` | アクセント塗りの要素(ボタン・日付ピル・アクティブタブ) |
| くぼみ | `0 2px 8px rgba(0,0,0,.4) inset` | 入力欄・preブロック |

## 5. タイポグラフィ

- フォント: **Noto Sans JP**(400/500/700/900)。`next/font` で読み込み(CJKのpreloadは無効・v1踏襲)
- ベース: `html { font-size: 17px }`、`line-height: 1.85`(カード本文)
- ウェイトの使い分け: 本文400 / 補助ラベル500 / 強調・ボタン700 / 見出し900
- サイズの目安: ページ見出し1.35rem / カード見出し1.22rem / 次回テーマ1.4rem / 本文0.95〜1rem / キャプション0.8〜0.82rem
- **eyebrow(小見出し)**: `.68rem / 700 / letter-spacing .22em / uppercase / アクセント色`。「Next Lesson / 次回の授業」のように英語+日本語の併記スタイル
- 数字・金額・日付は `tabular-nums`(v1踏襲)

## 6. 形状・モーション

### 角丸スケール

| 値 | 用途 |
|---|---|
| 12px | タブ・小要素・エラー帯 |
| 14px | 写真フレーム |
| 16px | 入力欄・主要ボタン・プロンプト枠 |
| 20px | カード標準(`--radius`) |
| 26px | 入口ボックス |
| 999px | ピル(日付・コピーボタン・トリコロール) |

### モーション

- ホバー: `translateY(-2〜-3px)` + 影を1段階強く。`transition: .12〜.15s`
- アクティブ: `translateY(0)` に戻す(またはv1の `active:scale-95`)
- タブ・カードなど、動くのは「押せるもの」だけ。装飾のためのアニメーションはしない

## 7. コンポーネント(生徒向け `/kiroku`)

正典はサンプル参照。ここでは構成と要点のみ。

- **入口ボックス(合言葉/クラスえらび共通)**: 中央配置・max-width 460px・ガラス面+blur+大影。トリコロール→英語small見出し→日本語見出し(900)→lead文(sub色)の順
- **合言葉入力**: 中央揃え1.35rem・くぼみ影。フォーカスで縁と `--accent-soft` の3pxリング。エラーはローズ帯でやさしい文言
- **クラスえらびグリッド**: 2列。各ボタン左端に5px縦バー(クラス色→濃色のグラデ)、曜日1.3rem/900+時間帯sub。ホバーで浮き+縁がクラス色に
- **ヘッダー**: sticky・`rgba(11,13,18,.82)`+blur(12px)・下1pxライン。ブランドマーク(38px角丸12・アクセントグラデ+グロー)+教室名(900)/英語small
- **クラスタブ**: 横並び(あふれたら横スクロール)。非選択=`rgba(255,255,255,.04)`面+sub文字、選択=クラス色グラデ塗り+白+色グロー。水曜は「水+small(午前/午後)」の2行表記。実装時は `min-height: 44px`
- **お知らせカード**: 琥珀の役割色一式+📢。eyebrowは「Information」
- **次回のじゅぎょうカード**: 最も目立つカード。クラス色の縁(45%)+右上からクラス色のradialグロー+surface-2→surfaceグラデ。eyebrow「Next Lesson / 次回の授業」、日付1.12rem/700、テーマ1.4rem/900、持ち物はsub
- **今月のよていカード**: ガラス面小。📅見出し+授業日テキスト、休みはローズ700
- **セクション見出し**: 1.2rem/900+右側にフェードする1pxライン(`linear-gradient(90deg, var(--line), transparent)`)
- **記録カード**: surface-2→surfaceグラデ+中影+上端ハイライト。日付=クラス色グラデのピル(白文字+色グロー)→タイトル1.22rem/900→メモ(text-body)→写真→プロンプト
- **写真フレーム**: 白背景・角丸14・1pxライン・影中。下部にsub色キャプション帯(surface面)
- **プロンプトブロック**: 紫の役割色一式。eyebrow「Prompt / この日AIにお願いした言葉」+くぼんだpre(文字 #e8e4ff)+コピーボタン(紫グラデのピル。押すと緑グラデ「✅ コピーしました」に2.5秒変化)
- **空状態**: 破線ボックス+sub色の前向きな文言(「これから増えていきます。お楽しみに!」)
- **フッター**: 中央・sub色・トリコロール付き。生徒さんへのやさしい一言

## 8. 管理画面への展開方針(段階刷新)

同じトークン・同じ立体感の文法を管理画面にも適用する。生徒向けとの違いは次のとおり。

- **アクセントは固定**: 管理画面の基本アクセントは水曜午前と同じスカイ `#38bdf8`(主要ボタンは `#38bdf8→#2563eb` グラデ=サンプルの「開く」ボタンと同一)。クラス文脈がある場所(コマ別表示・記録管理のクラス選択など)ではそのクラス色を使ってよい
- **出欠トグルの再定義(v1からの置き換え)**: 出席=アクセントグラデ塗り+白+チェック+色グロー / 欠席=`surface-2` 塗り+text / 未記録=ゴースト(透明+lineの縁+sub文字)。「出席だけが光る」関係性はv1と同じ
- **月次集計ヒーロー**: ダークタイル(#272729系)は廃止し、ページ全体がダークになるため「アクセントのradialグローを持つ大型カード」(次回カードと同じ文法)で金額を見せる
- **フォーム部品**: 入力欄=くぼみ影+フォーカスリング(4章・5章の式)。ラベル14px/700。エラー文言はローズ
- **ConfirmDialog / Toast**: 面はガラスカード+大影に置き換え。破壊的操作の確定ボタンはローズ系グラデ
- **段階移行のルール**: 画面単位で新デザインへ置き換える(要件定義書16章M3)。移行期間中の新旧混在は許容するが、**1画面内での混在は禁止**
- 未確定(実装時に決める): カレンダーグリッドのドット表現の色替え詳細 / PC用サイドバー・純黒トップバーの扱い(ダーク統一に伴い簡素化できる可能性)

## 9. 実装ガイド

- トークンは `tailwind.config.ts` の `theme.extend`(colors / borderRadius / boxShadow / backgroundImage)へ登録。**インラインhex禁止はv1から継続**
- クラスカラーはDBの `classes.theme_color` を CSS変数 `--accent`(または要素ローカルの `--c`)として `style` 属性で注入し、Tailwind側は `var(--accent)` 参照のユーティリティを用意する(動的hexをクラス名に埋め込まない)
- 濃色端・淡色面は `color-mix()` で導出する(全モダンブラウザ対応済み。らくらくスマホ系の古いブラウザで崩れないかはM1の実機確認項目)
- `backdrop-filter` はヘッダー・入口のみに限定(多用すると低スペック端末で重くなる)
- 影・グラデーションは4章のレシピ外の値を新造しない(増やしたくなったら本書を先に改訂する)

## 10. アクセシビリティ・禁止事項

- 本文文字は `text` / `text-body` を使用。`sub` は補助情報のみ(本文に使わない)
- クラス色の上に載せる文字は白固定(クラス色を文字色に使うのは短いラベル・eyebrowのみ)
- 禁止: 16px以下の本文 / 44px未満のタップ要素 / 役割色の流用 / レシピ外の影・グラデ新造 / 1画面内の新旧デザイン混在 / 装飾目的の常時アニメーション
- 文字サイズ変更ボタン・印刷スタイルは設けない(要件定義書どおり)
