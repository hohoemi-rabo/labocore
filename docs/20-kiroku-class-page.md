# 20. 生徒向け クラスページ（K3）

**依存**: 19（表示データの確認には 16〜18 の入力機能が先にあるとよい）
**参照**: REQUIREMENTS_phase2.md §7（K3・記録カード） / DESIGN_v2.md §7 / docs/design-sample.html
**マイルストーン**: M1

## 目的

生徒向けのメイン画面 `/kiroku/[classId]` を作る。サンプル `docs/design-sample.html` が見た目の正典。データ取得は anon クライアント（RLS で published・期間内・active のみ届く）。

## Todo

- [x] sticky ヘッダー: ブランドマーク（38px 角丸12・アクセントグラデ+グロー）+「ほほ笑みラボ / CLASS RECORD」・`rgba(11,13,18,.82)`+blur(12px)・下1pxライン
- [x] クラスタブ: 全 active クラスを横並び（あふれたら横スクロール）・**`min-height: 44px`**（サンプルからの引き上げ・DESIGN_v2 §0）。非選択=淡面+sub文字 / 選択中=クラス色グラデ塗り+白+色グロー。水曜は「水+small（午前/午後）」の2行表記。タブは Link 遷移で**記憶クラスの Cookie は変更しない**（次回も自分のクラスが開く）
- [x] 選択中クラスの `theme_color` を `--accent` / `--accent-soft` として style 属性で注入し、画面全体の差し色にする
- [x] ページ見出し: クラス名 1.35rem/900 + トリコロールバー（56px）
- [x] **お知らせ**: 全体向け（class_id IS NULL）+ このクラス向けを表示（掲載期間は RLS 済み・表示順は開始日降順）。琥珀の役割色一式+📢+eyebrow「Information」
- [x] **次回のじゅぎょうカード**: `next_lesson_*` を表示（`formatDateWithWeekday` + コマの時間帯）。**日付が今日（JST）より前なら表示しない**。クラス色の縁+radial グローの最も目立つカード
- [x] **今月のよていカード**: このクラスの曜日の当月開講日を列挙（「授業日: 3日・10日…」）。`closed_days` に該当する日は開講日から除き「お休み: 13日(木)は◯◯のためお休みです」をローズ 700 で表示（reason があれば添える）。日付処理は `format.ts`（`todayJst`/`weekdayOf`）経由で tz 依存を作らない
- [x] **これまでのじゅぎょう**: published の記録カードを新しい順に表示。日付ピル（クラス色グラデ・白文字・`formatDateWithWeekday`）→ テーマ 1.22rem/900 → メモ（text-body）→ 写真 → プロンプト
- [x] 写真表示: 白背景フレーム・角丸14・1pxライン・影中（R2 の URL を表示。next/image を使う場合は 15 の remotePatterns 前提）
- [x] プロンプトブロック: 紫の役割色・eyebrow「Prompt / この日AIにお願いした言葉」・くぼんだ pre。**コピーボタン**（紫グラデのピル → 押すと緑グラデ「✅ コピーしました」に 2.5 秒変化。clipboard API・ここだけクライアント部品）
- [x] 空状態: 破線ボックス+「このクラスの記録は、これから増えていきます。お楽しみに!」
- [x] フッター: トリコロール+やさしい一言+「クラスをえらびなおす」導線（Cookie を消して `/kiroku/select` へ）
- [x] 存在しない/廃止済み classId はクラスえらびへリダイレクト
- [x] **`/kiroku` 配下を `export const dynamic = "force-dynamic"` にする（重要・18 からの申し送り）**
  - 生徒向けは anon で読むため cookie クライアントを使わず、管理画面と違って**ルートキャッシュが効いてしまう**
  - お知らせの掲載期間と次回のじゅぎょうの過去日判定は**時刻で状態が変わるのに、日付をまたいでも何のミューテーションも起きないので `revalidatePath` が走らない**。そのままだと**期限切れのお知らせが生徒に表示され続ける**
  - RLS は毎リクエスト `now()` を評価するので、リクエストが DB に届きさえすれば正しく消える。届かせるのが `force-dynamic` の役目
  - 動作確認: 期限切れのお知らせを作り、`/kiroku/[classId]` を**リロードしても出てこない**こと（DB を直接叩く確認だけでは、このキャッシュの問題は見つからない）

## 実装時の決定（後から「直され」ないように）

サンプル・DESIGN_v2 から意図的に外した点と、その理由。

- **写真のキャプション帯は出さない。** DESIGN_v2 §7 は「下部に sub 色キャプション帯」と書いているが、`lesson_records` にキャプション列が無い。サンプルの帯はモック SVG を説明するための文章だった。語ることが無い帯は出さない
- **写真は枚数によらず縦積み。** 375px で2列にすると1枚150px 程度になり、横長スクリーンショットの文字が読めない
- **見出しは「これまでのじゅぎょう」**（サンプルの `h2` は「これまでの授業」）。管理画面の「次回のじゅぎょう」と表記を揃える。「見た目の正典はサンプル」は寸法の話で、文言はチケット側が優先
- **お休みの文言は「お休みです」に統一。** サンプルは過去日を「お休みでした」、未来日を「お休みです」と書き分けているが、過去日と未来日を区別しない実装なので統一する
- **記録カードから hover の浮き上がりを外した。** Tailwind 3.4 は `hoverOnlyWhenSupported` が既定オフで、タップすると hover が発火して貼り付く。記録カードは押せる要素ではない（DESIGN_v2 §6）。動かすのはタブ・コピーボタン・フッターだけ
- **クラスタブは `prefetch={false}`。** 表示中の Link が全部プリフェッチを撃つと、`force-dynamic` なルートを1回の閲覧でクラス数ぶん余計に実行する
- **コピーボタンには非 secure context 用のフォールバックが要る。** `navigator.clipboard` は `http://192.168.x.x` では **undefined** で、素直に呼ぶと TypeError で落ちる（実測確認済み）。`textarea` + `execCommand` に落とし、それも失敗したら ✅ を出さずに「コピーできませんでした。文字を長押しして選んでください。」を出す
  - **実機（PC・スマホ）でコピーできることを確認済み（2026-08-17）。** 本番は https なので secure context になり、通常は clipboard API 側を通る。フォールバックは LAN IP で開発確認するとき用の保険
- **`<pre>` に `font-jp` が必須。** Tailwind の preflight が `pre` に `fontFamily.mono` を当てるため、指定しないと日本語が等幅フォールバックで崩れる
- 「午前/午後」を出すかの判定は `src/lib/kiroku/classes.ts` の `periodLabels()` に一本化した（クラスえらび K2 とクラスタブ K3 で表記がずれないように）

## 完了条件

- [x] サンプルと同等の見た目・挙動でクラスページが表示され、管理画面の入力（記録・次回・お知らせ・休講日・テーマカラー）がすべて反映される
- [x] タブで他クラスを自由にのぞけて、差し色がクラスごとに切り替わる
- [x] スマホ実機（375px 幅）で崩れなく表示・操作できる
  - 375px のヘッドレスブラウザで29項目を確認済み（sticky・タブ44px・横スクロールなし・写真・コピーボタン）
  - **絵文字（📢 📅 📋 😊）はこの検証環境にフォントが無く豆腐になる**。実機での見え方はチケット21 で確認する
