# 20. 生徒向け クラスページ（K3）

**依存**: 19（表示データの確認には 16〜18 の入力機能が先にあるとよい）
**参照**: REQUIREMENTS_phase2.md §7（K3・記録カード） / DESIGN_v2.md §7 / docs/design-sample.html
**マイルストーン**: M1

## 目的

生徒向けのメイン画面 `/kiroku/[classId]` を作る。サンプル `docs/design-sample.html` が見た目の正典。データ取得は anon クライアント（RLS で published・期間内・active のみ届く）。

## Todo

- [ ] sticky ヘッダー: ブランドマーク（38px 角丸12・アクセントグラデ+グロー）+「ほほ笑みラボ / CLASS RECORD」・`rgba(11,13,18,.82)`+blur(12px)・下1pxライン
- [ ] クラスタブ: 全 active クラスを横並び（あふれたら横スクロール）・**`min-height: 44px`**（サンプルからの引き上げ・DESIGN_v2 §0）。非選択=淡面+sub文字 / 選択中=クラス色グラデ塗り+白+色グロー。水曜は「水+small（午前/午後）」の2行表記。タブは Link 遷移で**記憶クラスの Cookie は変更しない**（次回も自分のクラスが開く）
- [ ] 選択中クラスの `theme_color` を `--accent` / `--accent-soft` として style 属性で注入し、画面全体の差し色にする
- [ ] ページ見出し: クラス名 1.35rem/900 + トリコロールバー（56px）
- [ ] **お知らせ**: 全体向け（class_id IS NULL）+ このクラス向けを表示（掲載期間は RLS 済み・表示順は開始日降順）。琥珀の役割色一式+📢+eyebrow「Information」
- [ ] **次回のじゅぎょうカード**: `next_lesson_*` を表示（`formatDateWithWeekday` + コマの時間帯）。**日付が今日（JST）より前なら表示しない**。クラス色の縁+radial グローの最も目立つカード
- [ ] **今月のよていカード**: このクラスの曜日の当月開講日を列挙（「授業日: 3日・10日…」）。`closed_days` に該当する日は開講日から除き「お休み: 13日(木)は◯◯のためお休みです」をローズ 700 で表示（reason があれば添える）。日付処理は `format.ts`（`todayJst`/`weekdayOf`）経由で tz 依存を作らない
- [ ] **これまでのじゅぎょう**: published の記録カードを新しい順に表示。日付ピル（クラス色グラデ・白文字・`formatDateWithWeekday`）→ テーマ 1.22rem/900 → メモ（text-body）→ 写真 → プロンプト
- [ ] 写真表示: 白背景フレーム・角丸14・1pxライン・影中（R2 の URL を表示。next/image を使う場合は 15 の remotePatterns 前提）
- [ ] プロンプトブロック: 紫の役割色・eyebrow「Prompt / この日AIにお願いした言葉」・くぼんだ pre。**コピーボタン**（紫グラデのピル → 押すと緑グラデ「✅ コピーしました」に 2.5 秒変化。clipboard API・ここだけクライアント部品）
- [ ] 空状態: 破線ボックス+「このクラスの記録は、これから増えていきます。お楽しみに!」
- [ ] フッター: トリコロール+やさしい一言+「クラスをえらびなおす」導線（Cookie を消して `/kiroku/select` へ）
- [ ] 存在しない/廃止済み classId はクラスえらびへリダイレクト

## 完了条件

- サンプルと同等の見た目・挙動でクラスページが表示され、管理画面の入力（記録・次回・お知らせ・休講日・テーマカラー）がすべて反映される
- タブで他クラスを自由にのぞけて、差し色がクラスごとに切り替わる
- スマホ実機（375px 幅）で崩れなく表示・操作できる
