# 25. デザイン刷新: 今日の出欠・カレンダー

**依存**: 24
**参照**: DESIGN.md §8 / SPEC.md §7.2・§7.3・§9
**マイルストーン**: M3

## 目的

最重要画面（今日の出欠）とカレンダーを v2 化する。出欠トグルは v2 の定義に置き換えるが、楽観的更新・単価スナップショット等の挙動は一切変えない。

## Todo

- [x] 出欠トグルを v2 に再定義する: **出席=アクセントグラデ塗り+白+チェック+色グロー / 欠席=`surface-2` 塗り+text / 未記録=ゴースト（透明+line 縁+sub 文字）**。「出席だけが光る」関係性は v1 と同じ
  - **連結セグメントをやめ、2つの独立したピル**にした。1つの pill にまとめて `overflow-hidden` で囲むと、出席の色グローが切り取られるため（DESIGN §8 に追記済み）
- [x] `AttendanceBoard` / `AttendanceToggle` / `AddStudent` は共通部品のため、ホームとカレンダーパネルの**両方が同時に v2 に切り替わる**（このチケットで両画面をまとめて刷新する理由）
  - あわせてトーストを v1 → **v2 版**に差し替え（v1 は ink 塗りでダーク面に溶ける）、検索欄も v2 の `inputClass` に
- [x] ホーム（`/`）: 日付見出し・生徒リスト・「本日の記録完了」・休講日表示をガラスカードの文法で刷新する
  - 見出しは記録画面と同じ「見出し + トリコロール小」。完了表示は役割色の緑（`text-done`）、休講はローズ（`text-off`）
- [x] カレンダー: 月グリッド・記録ドット/休講日/今日/選択中の表現の色替え詳細を確定して実装する（DESIGN §8 の未確定事項 → **決定内容を DESIGN.md に追記する**）
  - **ドット表現を維持**（週6回運用では月の大半に記録が付くため、日を塗ると盤面が埋まる）。決定内容は DESIGN §8 に追記済み
- [x] 日付パネル（モバイル=下シート / PC=右パネル）をガラス面+大影で刷新する
  - 不透明カード面 + `shadow-elev-3`。`backdrop-blur` は使わない（DESIGN §9: ヘッダーと入口のみ）
- [x] 挙動の維持を確認: 楽観的更新（即時反映・失敗ロールバック+トースト）/ 別コマ生徒の表示 / 過去日の修正・追加 / 休講日は出欠 UI を出さない
  - `lib/attendance.ts` と `recordAttendance` は**一切変更していない**（見た目だけの変更）
- [x] 両画面から暫定白面ラッパーを外す
  - `calendar/layout.tsx` を削除。残る `LegacyPanel` は**集計と設定だけ**（26・27 で外す）

## 完了条件

- [x] 出欠記録の一連（タップ・追加・過去日修正・休講表示）が新デザインで機能し、動作がフェーズ1と同一
- [x] タップ要素 44px 以上・`active:scale-95` 等のフィードバックが維持されている
