# 22. AI 下書き生成（Google Gemini API）

**依存**: 16（15 の画像縮小を共用）
**参照**: REQUIREMENTS_phase2.md §9
**マイルストーン**: M2

## 目的

走り書きメモ+写真から、記録カードの「テーマ」「ひとことメモ」の下書きを Gemini API に作らせ、週6回運用の負荷を下げる。**AI はあくまで補助**であり、必ず人が確認・手直しして公開する。API 障害時も手動入力だけで完結できること。

## Todo

- [ ] env: `GEMINI_API_KEY`（サーバー専用・クライアントに露出させない）と `GEMINI_MODEL`（**モデル ID を差し替え可能に**。既定は現行世代の Flash 系。Gemini は旧世代の提供終了が早いためハードコード禁止）。`.env.local` と Vercel（Production）に設定（運用者タスク）
  - ローカルの `.env.local` は設定済み。**Vercel Production への `GEMINI_API_KEY` 設定が残っている**（`GEMINI_MODEL` は任意）
- [x] 実装着手前に最新の公式情報を確認する: SDK（`@google/genai` 系）の現行 API・無料枠の最新条件・推奨 Flash モデル ID（Context7 / Google AI Studio 公式ドキュメント）
  - SDK は `@google/genai` v2.17.1。無料枠のある Flash 系は 3.7 / 3.6 / 3.5 Flash と 3.5 / 3.1 Flash-Lite（2026-08 時点）。既定は `gemini-3.5-flash-lite`
- [x] Server Action `generateDraft`: 走り書きメモ+アップロード済み写真（`image.ts` で**縮小してから**送信・通信量節約）→ Gemini から「テーマ（短い見出し）」「ひとことメモ（です・ます調2〜3文）」を構造化して受け取る
  - 縮小（`AI_MAX_EDGE` = 768px）は `src/lib/gemini.ts` の中で必ず行う（呼び出し側が掲載用 1200px を誤って送る経路を作らない）
  - 保存済み写真は**フォームの URL を直接 fetch しない**。`updateRecord` と同じく DB の `image_urls` との積集合を採る
- [x] 生成プロンプトを設計しコードに定数として管理する: シニア向け教室の温かいトーン / です・ます調2〜3文 / テーマは短い体言止め / 個人名・個人情報を出力しない
  - `src/lib/gemini.ts` の `SYSTEM_INSTRUCTION`。出力は `responseSchema`（`{theme, memo}`）で構造化
- [x] `RecordForm` に「AIに下書きを作ってもらう」ボタンを追加: 実行中はローディング表示 → 成功でテーマ・メモ欄に反映（**編集可能なまま**・非制御入力への反映方法に注意）→ そのまま手直しして保存できる
  - `src/app/(app)/records/ai-draft-panel.tsx`。テーマ・メモだけ制御コンポーネント化して反映する
  - どちらかに入力済みのときは置き換え確認ダイアログを挟む（書きかけを事故で失わないため）
  - **写真の縮小中はボタンを無効化する**（縮小が終わるまで送信対象に入らないため、押せると写真なしで生成されてしまう）。AI に渡す枚数はボタンの下に常に表示する
- [x] エラーハンドリング: タイムアウト・レート制限（429）・API 障害は分かりやすい文言のトーストにする（握りつぶさない）。失敗しても手動入力での作成・公開は一切妨げない
  - `status` で分類（429 / 400・401・403 / 404 / 5xx / タイムアウト / 応答不正）。キー未設定はボタンを無効にして理由を表示
- [x] AI 出力の自動公開はしない（フォームに反映するだけ。公開は人の操作のみ・要件 §15）
- [x] 運用ルールを README または CLAUDE.md に明記する: **個人情報を含むスクショ・メモを AI に送らない**（無料枠の入力は Google の製品改善に利用されうるため）
  - README「AI 下書き（Gemini）」。入力欄のすぐ下にも同じ注意を出している
- [x] `npm run verify:gemini`（`scripts/verify-gemini.mts`）を追加。キー・モデル・画像付き生成をブラウザなしで確認できる（`verify:r2` と同じ流儀）

## 完了条件

- [x] メモ+写真から下書きが生成されフォームに反映され、手直し→公開の一連が数分で完了する
  - サーバー側の経路（縮小 → 生成 → 構造化出力の検証）は `npm run verify:gemini` で確認済み（約2秒 / 1,434 トークン）。**ブラウザでの通し確認は本番デプロイ後に行う**
- [x] `GEMINI_MODEL` の値を変えるだけでモデルを切り替えられる（存在しないモデル ID で 404 → 専用文言になることも確認）
- [x] API キー未設定・API 障害の状態でも、記録カードの手動作成・公開が問題なく行える
