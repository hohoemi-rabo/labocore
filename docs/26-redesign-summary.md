# 26. デザイン刷新: 月次集計

**依存**: 24
**参照**: DESIGN_v2.md §8 / SPEC.md §7.4
**マイルストーン**: M3

## 目的

月次集計のヒーローと支払いリストを v2 化する。集計ロジック（JS 集約・`unit_price_at_time` 合計）と楽観的更新は一切変えない。

## Todo

- [x] ダークタイルヒーロー（#272729 系・フルブリード）を廃止し、**「アクセントの radial グローを持つ大型カード」**（次回のじゅぎょうカードと同じ文法）で請求額合計を見せる（DESIGN_v2 §8）
  - `heroCardClass`（`v2/styles.ts` に追加）= `accentCardClass` と同じ面 + 影は §4 の「大」。
    **`${accentCardClass} shadow-elev-3` と後ろから足さない**（同じプロパティのユーティリティ2つは出力順まかせになる）
  - 巨大数値は `<Yen>` を流用せず手書き（`¥`=sub / 数字=fg）
- [x] 月送り（前月/翌月ボタン+月ラベル）を v2 の部品に置き換える
  - 44px の丸ボタン（`border-line`）。他画面と揃えて h1「月次集計」+ トリコロール小も追加
- [x] 支払いトグル pill を v2 化: 支払済=アクセントグラデ+チェック+色グロー / 未払い=ゴースト（transparent+line 縁+sub 文字）
- [x] `PaymentTable` の行・金額表示をガラスカードで刷新する（金額の `tabular-nums`・右揃えは維持。`<Yen>` の v2 上での見え方を確認し、必要なら調整）
  - `<Yen>` の「¥」を v1 の色トークンから **`opacity-60`** に変更。白面（生徒詳細・27 まで v1）とダーク面の**両方が使う部品**なので、親の文字色に対する相対表現にした
  - トーストも v2 版に差し替え
- [x] 「この月の出欠記録はありません。」等の空状態も v2 の文法に合わせる（破線ボックス + sub 文字）
- [x] 挙動の維持を確認: 月送り・支払いチェックの楽観的更新・退会者を含む集計対象
  - `page.tsx` の集計部分と `payment-actions.ts` は**一切変更していない**
- [x] 画面から暫定白面ラッパーを外す
  - `summary/layout.tsx` を削除。残る `LegacyPanel` は**設定だけ**（27 で外す）

## 完了条件

- [x] 集計値・支払いチェックの挙動がフェーズ1と同一のまま、新デザインで表示される
