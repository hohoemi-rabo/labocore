# 17. 管理: 次回のじゅぎょう編集 + コマのテーマカラー選択

**依存**: 14, 16（ナビ・`/records` 配下を使う）
**参照**: REQUIREMENTS_phase2.md §6・§8.2 / DESIGN_v2.md §3
**マイルストーン**: M1

## 目的

クラスごとの「次回のじゅぎょう」（日時・テーマ・持ち物メモ）を編集できるようにする。あわせて、生徒向け画面の差し色になる `theme_color` をコマ管理から変更できるようにする。

## Todo

- [ ] コマ管理フォーム（`ClassForm`）にテーマカラー選択欄を追加する: DESIGN_v2 §3 の6色をスウォッチで候補提示し選択（自由入力は不要）。コマ管理画面全体は v1 のままなので、追加欄も v1 スタイルで馴染ませる（画面全体の v2 化は 27 で実施）
- [ ] `/records/next-lessons`: 全 active コマの「次回のじゅぎょう」をまとめて編集する画面（v2 デザイン）。クラスごとに 日付・テーマ・持ち物メモ の3項目で `classes.next_lesson_*` を更新する
- [ ] Zod 検証: テーマを入れるなら日付必須、などの整合を取る。「未定に戻す」（3項目クリア）も可能にする
- [ ] 過去日になった次回情報は生徒向けに表示しない（表示制御の実装は 20。ここではデータを消さず残す仕様であることを明記）
- [ ] 変更成功時は `/records` 系と `/kiroku` 系を `revalidatePath` する

## 完了条件

- コマごとに次回のじゅぎょうを設定・変更・クリアできる
- コマ管理からテーマカラーを変更でき、（20 実装後）生徒向け画面の差し色に反映される
